# Copy this file to deploy.config.sh (gitignored) and fill in your target.
# Used only by the manual deploy.sh fallback; CI deploys via GitHub Actions secrets.
HOST="user@example.com"
WEBROOT="/var/www/yoursite"
